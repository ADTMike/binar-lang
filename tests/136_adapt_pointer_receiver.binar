// exit: 0
// Tests: pointer receiver with structural type
import { os } from "os"

type ZapLogger {
    X int
}

fn SetVal(l *ZapLogger) {
    l.X = 99
}

fn Process(l ~ZapLogger) int {
    l.SetVal()
    return 0
}

fn main() {
    z := ZapLogger{X: 0}
    Process(z)
    os.Exit(z.X)
}
