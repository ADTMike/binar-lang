// exit: 0
// Tests: constants with iota, export convention
import { os } from "os"

fn check(expected int, actual int) {
    if expected != actual {
        os.Exit(1)
    }
}

fn main() {
    check(0, 0)
}
