// exit: 42
// Tests: basic structural dispatch with method call
import { os } from "os"

type ZapLogger {
    X int
}

fn GetVal(l ZapLogger) int {
    return l.X
}

fn Process(l ~ZapLogger) int {
    return l.GetVal()
}

fn main() {
    z := ZapLogger{X: 42}
    os.Exit(Process(z))
}
