// exit: 0
// Test: tail recursion inside if/else branches

fn count_down(n int) error {
    if n <= 0 {
        return nil
    } else {
        return count_down(n - 1)
    }
}

fn main() int {
    err := count_down(10)
    if err != 0 {
        return 1
    }
    return 0
}
