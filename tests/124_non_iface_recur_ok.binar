// exit: 5
// Test: non-port recursion should work unchanged
// Fib(5) = 5 (Fibonacci)
import { os } from "os"

fn Fib(n int) int {
    if n <= 1 {
        return n
    }
    return Fib(n - 1) + Fib(n - 2)
}

fn main() {
    os.Exit(Fib(5))
}
