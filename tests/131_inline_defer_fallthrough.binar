// exit: 0
// Test: defer inside inlined function without explicit return executes on fall-through

type Myerr {
    code int
}

fn Error(e Myerr) string {
    return "error"
}

fn store(addr *int, val int) {
    *addr = val
}

fn noop(flag *int) error {
    defer store(flag, 77)
    // no explicit return — falls through
}

fn main() {
    flag := 0
    noop(&flag)
    if flag != 77 {
        os.Exit(1)
    }
    os.Exit(0)
}
