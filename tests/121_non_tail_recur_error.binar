// exit: 0
// Test: tail recursive error-returning function (no longer blocked by sema)
import { os } from "os"

fn f(n int) error {
    if n <= 0 {
        return nil
    }
    return f(n - 1)
}

fn main() {
    f(10) raise
    os.Exit(0)
}
