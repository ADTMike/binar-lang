// exit: 0
// Test: mutual recursion between error-returning functions (no longer blocked by sema)
import { os } from "os"

fn a() error {
    b() raise
    return nil
}

fn b() error {
    a() raise
    return nil
}

fn main() {
    os.Exit(0)
}
