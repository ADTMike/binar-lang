// exit: 24
import { os } from "os"
fn double(x int) int {
    return x * 2
}

fn add_to(x int, y int) int {
    return x + y
}

fn main() {
    x := 7
    a := x.double()
    b := x.add_to(3)
    os.Exit(a + b)
}
