// Hello World in binar
// Compile: binar -o hello.o hello_world.binar
// Link:    gcc -nostartfiles -o hello hello.o

import { os } from "os"
import { fmt } from "fmt"

fn main() {
    fmt.Println("hello world")
    os.Exit(0)
}
